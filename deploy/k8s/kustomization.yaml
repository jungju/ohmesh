apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: ohmesh

resources:
  - namespace.yaml
  - configmap.yaml
  - pvc.yaml
  - deployment.yaml
  - service.yaml
  - ingress.yaml

images:
  - name: ghcr.io/jungju/ohmesh
    newTag: main
